# Cimple Compiler

This repository contains the Cimple compiler scaffold. Cimple's language syntax follows Python syntax and the compiler produces native binaries via LLVM.

See the full specification: SPECIFICATION.md
