# Cimple Compiler

This repository contains the Cimple compiler scaffold. Cimple's language follows the same syntax as Python and the compiler produces native binaries via LLVM.

See the full specification: SPECIFICATION.md
