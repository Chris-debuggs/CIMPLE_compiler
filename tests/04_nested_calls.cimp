# Test 4: Nested function calls and expressions
# Tests: function calling another function, chained arithmetic
def square(n)
    return n * n

def sum_of_squares(a, b)
    return square(a) + square(b)

print(square(4))
print(sum_of_squares(3, 4))
